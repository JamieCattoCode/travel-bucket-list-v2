import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  sidebar: {
    backgroundColor: 'black',
    height: '100%',
  },

  link: {
    color: 'white',
    textDecoration: 'none',
  },
}));
